import React from "react";
import classes from './UserProfile.module.css';

const AdminProfile = () => {
    return (
        <section className={classes.starting}>
          <h1>Welcome Admin Profile Page!</h1>
        </section>
      );
};

export default AdminProfile;
